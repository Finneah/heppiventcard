// import {
//   TourGuideZone, // Main wrapper of highlight component
//   useTourGuideController, // hook to start, etc.
// } from 'rn-tourguide';
// jest.mock('rn-tourguide', () => {
//   return {
//     TourGuideProvider: jest.fn().mockImplementation((x) => x),
//   };
// });

// jest.mock('vasern', () => {
//   return jest.fn().mockImplementation(() => ({
//     vasern: jest.fn().mockImplementation((x) => x),
//     Queryable: jest.fn().mockImplementation((x) => x),
//     Document: jest.fn().mockImplementation((x) => x),
//     validateProps: jest.fn().mockImplementation((x) => x),
//   }));
// });
